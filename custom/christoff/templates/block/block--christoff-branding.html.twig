{% extends "block.html.twig" %}

{% block content %}
  <div class="logo-content">

    {% if site_logo %}
      <a href="{{ path('<front>') }}" title="{{ 'Home'|t }}" rel="home">

        <img height="200" width="auto" src="{{ site_logo }}" alt="{{ 'Home'|t }}" />

      </a>
    {% endif %}

  </div>
{% endblock %}
